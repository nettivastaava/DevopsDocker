nginx.conf:

  events { worker_connections 1024; }

  http {
    server {
      listen 80;

      location / {
        proxy_pass http://front-end:5000/;
      }

      location /api/ {
        proxy_pass http://back-end:8000/;
      }
    }
  }
